<!DOCTYPE html>
<html>
<head>
    <title>Editar Curso</title>
</head>
<body>
    <h1>Editar Curso</h1>
    <form method="POST" action="/cursos/<%= curso.curso_id %>">
        <input type="text" name="title" value="<%= curso.title %>" required>
        <textarea name="description" required><%= curso.description %></textarea>
        <input type="text" name="professor" value="<%= curso.professor %>" required>
        <input type="number" name="duration" value="<%= curso.duration %>" required>
        <button type="submit">Salvar</button>
    </form>
</body>
</html>